diff --git a/gcc/config/riscv/riscv.md b/gcc/config/riscv/riscv.md
index 3fe0ad0ccdf..84f179c0b8e 100644
--- a/gcc/config/riscv/riscv.md
+++ b/gcc/config/riscv/riscv.md
@@ -4960,6 +4960,22 @@ (define_split
   { operands[3] = GEN_INT (BITS_PER_WORD
                           - exact_log2 (INTVAL (operands[3]) + 1)); })

+(define_split
+  [(set (match_operand:DI 0 "register_operand")
+       (sign_extend:DI
+         (any_shift:SI (match_operand:SI 1 "register_operand")
+                       (subreg:QI (minus:SI (match_operand 2 "const_int_operand")
+                                            (match_operand:SI 3 "register_operand")) 0))))
+   (clobber (match_operand:DI 4 "register_operand"))]
+  "(TARGET_64BIT && INTVAL (operands[2]) == 31)"
+  [(set (match_dup 4) (not:DI (match_dup 3)))
+   (set (match_dup 0) (sign_extend:DI (any_shift:SI (match_dup 1) (match_dup 5))))]
+{
+  operands[3] = gen_lowpart (DImode, operands[3]);
+  operands[5] = gen_lowpart (QImode, operands[4]);
+})
+
+
