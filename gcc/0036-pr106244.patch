diff --git a/gcc/config/riscv/riscv.md b/gcc/config/riscv/riscv.md
index 1a180e3af226..1d328cf2a73e 100644
--- a/gcc/config/riscv/riscv.md
+++ b/gcc/config/riscv/riscv.md
@@ -5211,6 +5211,17 @@ (define_split
    (set (match_dup 0) (ashiftrt:X (match_dup 4) (match_dup 2)))]
   "{ operands[5] = gen_int_mode (HOST_WIDE_INT_1U << (BITS_PER_WORD - 1), word_mode); } ")
 
+(define_split
+  [(set (match_operand:DI 0 "register_operand")
+        (and:DI (subreg:DI (ashift:SI (const_int 1)
+                                      (match_operand:QI 1 "arith_operand")) 0)
+                (const_int 1)))
+   (clobber (match_operand:DI 2 "register_operand"))]
+  "TARGET_64BIT"
+  [(set (match_dup 2) (and:DI (match_dup 1) (const_int 63)))
+   (set (match_dup 0) (eq:DI (match_dup 2) (const_int 0)))]
+  { operands[1] = gen_lowpart (DImode, operands[1]); })
+
 ;; Standard extensions and pattern for optimization
 (include "bitmanip.md")
 (include "crypto.md")
