diff --git a/gcc/config/riscv/riscv.md b/gcc/config/riscv/riscv.md
index 1ec15da2e77e..9977c2d0f327 100644
--- a/gcc/config/riscv/riscv.md
+++ b/gcc/config/riscv/riscv.md
@@ -1898,8 +1898,7 @@ (define_insn_and_split "*zero_extendsidi2_internal"
   "@
    #
    lwu\t%0,%1"
-  "&& reload_completed
-   && REG_P (operands[1])
+  "&& REG_P (operands[1])
    && !paradoxical_subreg_p (operands[0])"
   [(set (match_dup 0)
 	(ashift:DI (match_dup 1) (const_int 32)))
@@ -1925,8 +1924,7 @@ (define_insn_and_split "*zero_extendhi<GPR:mode>2"
   "@
    #
    lhu\t%0,%1"
-  "&& reload_completed
-   && REG_P (operands[1])
+  "&& REG_P (operands[1])
    && !paradoxical_subreg_p (operands[0])"
   [(set (match_dup 0)
 	(ashift:GPR (match_dup 1) (match_dup 2)))
